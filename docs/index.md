---
page_title: "openprovider Provider"
description: |-
  The OpenProvider provider manages domains in Openprovider.
---

# openprovider Provider

The OpenProvider provider manages domains in Openprovider.

## Example Usage

```terraform
terraform {
  required_providers {
    openprovider = {
      source  = "charpand/openprovider"
      version = ">= 0.1.0"
    }
  }
}

provider "openprovider" {
  username = var.openprovider_username
  password = var.openprovider_password
}
```

## Authentication

The provider requires `username` and `password` configuration.

<!-- schema generated by tfplugindocs -->
## Schema

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `password` (String, Sensitive) OpenProvider password.
- `username` (String) OpenProvider username.


